/* eslint-disable require-jsdoc */
class Layout {
    constructor(measurements, settings) {
        this.measurements = measurements;
        this.settings = settings;
    }
}

export default Layout;
